# copy to .env.local and add your keys
GROQ_API_KEY=
CF_ACCOUNT_ID=
CF_API_TOKEN=
ELEVENLABS_API_KEY=

# optional — render workflows (image generation runs as a distributed workflow)
# set these on Render only; omit them locally to use the inline pipeline
RENDER_API_KEY=
PLAYBOOK_WORKFLOW_SLUG=
INGEST_SECRET=
WEB_SERVICE_URL=https://playbook-ai.onrender.com
